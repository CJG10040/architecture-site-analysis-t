# 프로젝트 선택 및 AI 설계 전환 제안 개선 계획

## 목표

사용자가 프로젝트를 찾거나 생성하지 못해 설계 전환 카드를 저장할 수 없는 문제를 해소한다. 동시에 `발견을 설계로` 영역의 기본 역할을 사용자 입력 양식에서 **AI가 근거 기반의 설계 전환 후보를 먼저 제시하고, 사용자가 선택·수정·저장하는 작업 흐름**으로 바꾼다.

## 확인된 현재 상태

현재 화면은 좌측 프로젝트 영역에서만 새 프로젝트 다이얼로그를 열 수 있고, 선택된 프로젝트가 없으면 카드 저장 시 단순히 `프로젝트를 먼저 선택하세요`라는 오류만 표시한다. 우측 `발견을 설계로` 영역은 핵심 키워드·주장·근거·설계 적용을 사용자가 전부 직접 작성해야 한다. 반면 서버에는 이미 명시적 클릭으로 실행되는 구조화 AI 보고서가 있으며, 설계 가설·근거·긴장·공간적 조치·위험·추가조사를 생성할 수 있다.

| 문제 | 사용자 영향 | 계획상 해결책 |
|---|---|---|
| 프로젝트 생성·선택 진입이 좌측에만 있음 | 카드 저장이 차단되고 원인을 찾기 어려움 | 작업대 상단과 설계 전환 영역에 항상 보이는 프로젝트 선택·새 프로젝트 버튼을 제공한다. |
| 선택 프로젝트 상태가 불명확함 | 현재 어떤 조사 단위에 기록되는지 알 수 없음 | 제목·상태·전환 메뉴를 상단에 명확히 표시한다. |
| 설계 전환 카드가 빈 양식부터 시작함 | 사용자가 AI 지원 도구 대신 직접 논리를 써야 함 | 사용자 명시 클릭으로 AI 후보를 생성하고, 선택한 후보를 카드 초안으로 적용한다. |

## 구현 단계

### 1. 프로젝트 선택·생성 진입을 항상 노출한다

작업대의 중앙 상단과 `발견을 설계로` 영역에 프로젝트 상태 컴포넌트를 추가한다. 활성 프로젝트가 없을 때는 `프로젝트 시작` 버튼과 짧은 안내를 표시하며, 이 버튼은 기존 프로젝트 생성 다이얼로그를 연다. 프로젝트가 있을 때는 현재 프로젝트 제목과 `프로젝트 변경`·`새 프로젝트` 동작을 표시한다.

프로젝트 목록이 하나 이상이면 선택기를 항상 노출하고, 목록이 없으면 바로 새 프로젝트 생성 흐름을 연다. 생성 성공 후에는 목록을 재조회하고 생성된 프로젝트를 활성 프로젝트로 설정하며, 카드·대지·AI 작업이 즉시 해당 프로젝트를 사용하도록 동기화한다. 프로젝트가 없는 경우 카드 저장 버튼을 단순 오류로 끝내지 않고 프로젝트 시작 다이얼로그를 열도록 변경한다.

### 2. AI 설계 전환 제안을 우측 패널의 기본 행동으로 만든다

`발견을 설계로` 영역 상단에 **`AI 설계 전환 제안 받기`** 버튼을 배치한다. 이 동작은 반드시 사용자 클릭으로만 실행하며, 기존 `reports.generate` 절차와 구조화 보고서의 `hypotheses`·`interpretations`·`designQuestions`를 재사용한다. 따라서 새 AI 모델·별도 자격 증명·자동 실행은 추가하지 않는다.

AI 실행 전에는 활성 프로젝트와 최소한의 대지 정보 또는 수집·관찰 자료 여부를 확인한다. 자료가 부족해도 AI는 사실·미확인·추가조사를 분리해 제한적인 질문과 가설만 제시하도록 기존 보고서 규칙을 유지한다. 처리 중에는 버튼 상태와 안내를 표시하고, 결과 생성 실패는 재시도 가능 메시지로 전달한다.

### 3. 후보 선택·검토·카드 저장 흐름을 구현한다

생성 결과에서 서로 다른 설계 전환 후보를 카드형으로 표시한다. 각 후보에는 제목, 근거, 긴장 또는 기회, 공간적 조치, 기대 경험, 위험, 추가조사와 사실·해석·가설 구분을 함께 보여 준다. 사용자는 `이 제안 사용`을 눌러 후보 내용을 기존 설계 전환 카드 초안에 채우고, 필요하면 직접 수정한 뒤 저장한다.

직접 작성은 보조 선택지로 유지하되 처음에는 접어 두거나 `직접 작성` 버튼을 통해 연다. 후보를 저장한 뒤에는 기존 AI 검토 상태(동의·부분 동의·반대·추가조사)를 기록할 수 있게 연결해, AI 결과가 확정 설계안이 아니라 검토 가능한 사고 재료임을 명확히 한다.

### 4. 데이터 출처와 저장 상태를 보강한다

후보 카드에는 사용된 조사 근거의 출처·수집 시각·한계가 있는지 표시한다. AI가 근거를 만들지 않도록 기존 구조화 스키마와 시스템 프롬프트의 사실·사용자 관찰·해석·가설·미확인 분리 규칙을 유지한다. 카드 저장 성공 후에는 활성 프로젝트 제목, 저장된 카드 수, 최근 저장 시각을 UI에 반영한다.

## 검증 계획

| 검증 항목 | 방법 | 기대 결과 |
|---|---|---|
| 프로젝트 없음 | 우측 또는 상단의 카드 저장·AI 제안 버튼 클릭 | 프로젝트 시작 버튼 또는 생성 다이얼로그가 즉시 나타난다. |
| 프로젝트 생성·선택 | 새 프로젝트를 만들고 목록에서 전환 | 활성 프로젝트 제목이 표시되고 후속 저장의 `projectId`가 일치한다. |
| AI 명시 실행 | `AI 설계 전환 제안 받기`를 클릭 | 기존 조사 자료를 바탕으로 구조화된 복수 후보가 표시되며, 클릭 전에는 AI가 실행되지 않는다. |
| 후보 적용 | AI 후보의 `이 제안 사용`을 클릭 후 저장 | 카드 초안에 내용이 채워지고 선택한 프로젝트의 카드로 저장된다. |
| 근거 부족·오류 | 대지 또는 수집 자료가 부족한 프로젝트에서 실행 | 사실과 미확인을 구분한 안내 또는 재시도 가능한 오류가 표시된다. |
| 자동 검사 | `pnpm check`, `pnpm test`, `pnpm build` | 타입 검사, 기존 권한·AI 입력·경계 테스트와 빌드가 통과한다. |

## 가정과 위험

AI 제안은 사용자 클릭 시에만 실행하며, 기존 구조화 보고서가 이미 제공하는 `gpt-5-mini` 기본 모델과 서버 측 호출 경로를 재사용한다. 사용자가 원하는 것은 AI가 설계 논리의 출발점을 제시하는 것이며, 최종 설계 판단과 법규·인허가 판단을 대신하는 것이 아니다. 활성 프로젝트가 없는 상태에서는 AI 생성과 카드 영속화 대상이 없으므로, 프로젝트 시작 흐름을 먼저 요구하는 것이 필요하다.
