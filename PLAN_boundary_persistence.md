# 정확 대지 경계 저장 신뢰성 개선 계획

## 목표

지도에서 완성한 대지 경계 폴리곤이 화면 표시와 별개로 프로젝트의 `sites.boundaryGeoJson`에 **확실히 저장되고, 새로고침 후에도 동일하게 복원**되도록 한다. 사용자가 경계 완성 직후 별도의 저장 단계를 놓치더라도 데이터가 유실되지 않게 하며, 저장 실패는 화면에서 명확히 안내한다.

## 확인된 현재 흐름과 가설

현재 작업대는 `경계 완성` 시 GeoJSON을 React의 `siteDraft` 상태에만 반영하고, 사용자가 상단의 `대지 저장`을 별도로 눌렀을 때에만 `sites.save` 요청을 보낸다. 서버는 GeoJSON을 정규화한 뒤 `sites` 테이블에 업데이트하는 구조이며, 경계 자체에 대한 서버 검증도 존재한다. 따라서 저장 누락은 주로 다음 두 경우에서 발생할 수 있다.

| 구분 | 가능한 원인 | 개선 방향 |
|---|---|---|
| 저장 단계 분리 | 사용자가 `경계 완성`만 수행하고 상단 `대지 저장`을 누르지 않음 | 경계 완성 시 자동 저장을 수행한다. |
| 상태 반영 시점 | 상태 갱신 직후 수동 저장을 누르면 이전 `siteDraft` 스냅샷이 전송될 가능성 | 저장할 경계·중심 좌표를 명시적인 다음 상태 스냅샷으로 만들어 전송한다. |
| 실패 피드백 부족 | 권한·서버 검증·네트워크 실패가 경계 화면에서 충분히 드러나지 않음 | 저장 중·저장 완료·실패 상태와 재시도 동작을 경계 패널에 표시한다. |

## 구현 단계

### 1. 저장 경로를 단일화한다

`Home.tsx`에 사이트 저장을 위한 공통 헬퍼를 만든다. 이 헬퍼는 `siteDraft`를 임의로 참조하지 않고, 주소·좌표·반경·경계 GeoJSON을 포함한 완성된 사이트 스냅샷을 인자로 받아 `sites.save` mutation으로 전달한다. 정상 응답 후에는 프로젝트 번들을 다시 불러와 서버에 저장된 경계를 기준으로 화면을 동기화한다.

### 2. 경계 완성 직후 자동 저장을 추가한다

`finishBoundaryDrawing`에서 최소 세 정점과 GeoJSON 유효성을 통과하면 다음 사이트 스냅샷을 만든다. 이때 폴리곤 중심점을 계산하여 경계 중심 좌표도 함께 반영한다. 활성 프로젝트가 존재하면 해당 스냅샷을 즉시 저장하고, 프로젝트가 아직 없으면 로컬 상태만 유지한 뒤 프로젝트 생성·선택 후 수동 저장을 안내한다.

자동 저장 중에는 `경계 저장 중` 상태를 표시하고, 성공하면 `경계 저장됨`과 저장 시각을 보여 준다. 실패하면 현재 경계는 지도에 그대로 남겨 두고, 오류 메시지와 `다시 저장` 버튼을 제공한다. 상단의 기존 `대지 저장`은 주소·반경·수동 편집 변경을 저장하는 보조 동작으로 유지한다.

### 3. 편집·삭제와 저장 상태를 일관되게 처리한다

기존 폴리곤의 정점을 드래그하거나 삽입·삭제해도 새 GeoJSON을 만들고 저장 대기 상태를 표시한다. 사용자에게 예상 밖의 과도한 요청을 만들지 않도록 편집 종료 후 짧게 묶어 저장하거나, 명확한 `변경사항 저장` 버튼을 제공하는 방식을 적용한다. 경계를 지우는 동작 역시 빈 GeoJSON을 서버에 저장해 새로고침 후 삭제 상태가 유지되도록 한다.

### 4. 서버 계약과 오류 피드백을 보강한다

`sites.save`는 유효한 GeoJSON의 정규화 결과를 그대로 반환하도록 확장하거나, 최소한 저장된 경계 존재 여부와 갱신 시각을 반환한다. 클라이언트는 성공 응답을 기준으로 저장 상태를 확정한다. 유효하지 않은 링·권한 오류·네트워크 오류는 기존 GeoJSON을 삭제하지 않고 사용자 이해가 가능한 메시지로 표시한다.

## 검증 계획

| 검증 항목 | 방법 | 기대 결과 |
|---|---|---|
| 경계 완성 자동 저장 | 지도에서 3개 이상 점을 찍고 `경계 완성` 실행 | 별도 상단 저장 없이 `boundaryGeoJson`이 서버에 저장된다. |
| 새로고침 복원 | 경계 저장 후 작업대를 새로고침 | 같은 폴리곤, 중심 좌표, 면적·둘레가 복원된다. |
| 정점 편집 | 저장된 폴리곤의 꼭짓점을 드래그·삽입·삭제 | 변경 상태와 저장 결과가 일치한다. |
| 삭제 영속성 | 경계를 지우고 재진입 | 기존 GeoJSON이 다시 나타나지 않는다. |
| 자동 검사 | `pnpm check`, `pnpm test`, `pnpm build` 실행 | 타입 검사·경계 GeoJSON 테스트·배포 빌드가 통과한다. |

## 가정과 위험

이 계획은 활성 프로젝트가 선택돼 있어야 DB 저장이 가능하다고 가정한다. 프로젝트가 없는 상태에서는 경계를 즉시 서버에 보관할 대상이 없으므로, 프로젝트 생성 또는 선택을 요구하는 명확한 안내를 유지한다. 또한 지도 클릭·GeoJSON 생성 자체는 현재 동작한다고 전제하며, 실행 단계에서 로그인된 작업대에서 실제 저장·새로고침 복원까지 확인한다.
